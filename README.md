# Meme Stock Discord Bot

Discord bot + admin API. Deploy on [Railway](https://railway.app) with `python run.py`.

## Railway

Set env from `.env.example`. Generate a public URL for the CRM (`VITE_API_URL`).

CRM dashboard: [discord-crm](https://github.com/chay0354/discord-crm)
