# Meme Stock Discord Bot

Discord weekly stock-pick game with Supabase, Stripe billing, and a CRM API (`run.py`).

Deploy on Railway: `python run.py` at repo root. See `.env.example` for required variables.
